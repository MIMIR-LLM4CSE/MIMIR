# Core MCP SDK and LLM backends
mcp>=1.0.0,<2
ollama>=0.3.0
openai>=1.0.0
anthropic>=0.116.0
httpx>=0.27.0

# WebSocket server (VS Code extension integration)
websockets>=12.0

# Scientific / utility servers
numpy>=1.24.0
sympy>=1.12
psutil>=5.9.0

# Optional: dev tooling — the two checks CI runs (`pip install -e ".[dev]"`)
# pytest>=8.0.0
# ruff>=0.6.0

# Optional: fine-tuning stack (only needed for the ml/ servers)
# torch>=2.0.0
# transformers>=4.40.0
# peft>=0.10.0
# datasets>=2.18.0
# trl>=0.8.0
# bitsandbytes>=0.43.0